# GPU runtime stack for this repo.
#
# Install in this order on a clean Python 3.10 virtualenv:
# 1. pip install --upgrade pip setuptools wheel
# 2. pip install -r requirements.txt
# 3. pip install --upgrade --force-reinstall --index-url https://download.pytorch.org/whl/cu121 -r requirements-gpu.txt
#
# If the environment has drifted badly, it is safer to recreate the venv
# rather than mixing old CPU wheels with these packages.

# CUDA PyTorch stack
torch==2.1.2+cu121
torchvision==0.16.2+cu121
torchaudio==2.1.2+cu121

# Pins to keep the HF / torch stack stable for this repo
numpy==1.26.4
transformers==4.38.2
sentence-transformers==2.6.1
datasets==2.17.1
fsspec==2023.10.0
tokenizers==0.15.2
accelerate==0.27.2
peft==0.9.0
safetensors==0.4.2
requests==2.31.0
charset-normalizer==3.4.7
urllib3==1.26.18
Pillow==10.2.0
typing-extensions==4.15.0
